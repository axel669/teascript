###
Try/Catch/Finally/Throw all operate as they do in JS.
Only difference is syntax, where catch and finally are blocks within a try
block.
###

try {
    # stuff

    throw "broken"

    @catch {
    }
}

try {
    # stuff

    @catch {
    }
    @finally {
    }
}
