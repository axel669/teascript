# console.log(
#     [0 -> 1000]
#         .filter(
#             fn{n} => (n % 3) == 0 || (n % 5) == 0
#         )
#         .reduce(
#             fn{total, n} => total + n
#             0
#         )
# )

# async fn test {
#     @items

#     => await.all items.map(
#         fn{item} => item.asyncThing()
#     )
# }

console.log(
    10
    |> (n: n + 1)
    |> (n: (n * 2) + n)
    |> (n: fn => n)
)

###
test
###
