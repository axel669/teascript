let x = "whole string"
mut actions <- {
    :a 10
    :b 12
    :"test" "other"
    :(x) 100
}
let arr <- [x ** 2 from x : 0 -> 100]

console.log(
    arr.length
)
console.log(
    arr.join(" ")
)

let y = "#{x} woah ${test}"
let z = "${test} maybe?"

tag`"SELECT * FROM #{table}"
tag`"another one"

export mut test = 100
export {
    test as wat
    other
    nope as woah
}
